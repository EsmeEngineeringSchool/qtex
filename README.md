# QTeX

Monorepo contenant les trois sous-modules QTeX :

- `qtex2xml` : conversion QTeX → XML
- `qtex2latex` : conversion QTeX → LaTeX
- `xml2xml` : transformations XML → XML (traduction)


Note: Il correspond à la fusion des trois dépôts qui étaient sur Github
